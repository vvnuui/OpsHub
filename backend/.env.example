# ==================== 服务器配置 ====================
# 服务器运行端口
PORT=3000

# 运行环境：development（开发）/ production（生产）
NODE_ENV=development

# ==================== 安全配置 ====================
# JWT 令牌密钥（生产环境必须修改为复杂的随机字符串）
JWT_SECRET=opshub-jwt-secret-key-change-in-production

# JWT 访问令牌有效期（秒）- 默认 2 小时
JWT_ACCESS_EXPIRES_IN=7200

# JWT 刷新令牌有效期（秒）- 默认 7 天
JWT_REFRESH_EXPIRES_IN=604800

# bcrypt 密码加密轮数（推荐值：10-12）
BCRYPT_SALT_ROUNDS=10

# ==================== SSO 单点登录配置 ====================
# SSO 加密密钥（生产环境必须修改，需要与 OA 系统保持一致）
SSO_SYS_KEY=vjDPXzvbQmI5GPv

# SSO 加密盐值（生产环境必须修改，需要与 OA 系统保持一致）
SSO_SALT=hWiqER1nLeAtQrN

# ==================== 数据库配置 ====================
# SQLite 数据库文件路径（相对于 backend 目录）
DATABASE_PATH=./data/opshub.db

# ==================== 前端配置 ====================
# 前端构建产物路径（相对于 backend/src 目录）
FRONTEND_DIST_PATH=../../frontend/dist

# ==================== 健康检查配置 ====================
# 系统健康检查间隔时间（毫秒）- 默认 30 秒
HEALTH_CHECK_INTERVAL=30000

# 健康检查超时时间（毫秒）- 默认 5 秒
HEALTH_CHECK_TIMEOUT=5000

# ==================== 初始管理员账户 ====================
# 默认管理员用户名
DEFAULT_ADMIN_USERNAME=admin

# 默认管理员密码（首次运行后建议立即修改）
DEFAULT_ADMIN_PASSWORD=admin123

# 默认管理员邮箱
DEFAULT_ADMIN_EMAIL=admin@opshub.local
